# Use postgres/example user/password credentials
version: '3.2'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: dev
      POSTGRES_USER: 'anton.bozhinov'
      POSTGRES_PASSWORD: 'Domini1313'
    volumes:
      - ./db/postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432
    networks:
      - backend
  
  postgresAdmin: 
    image: adminer
    restart: always
    ports:
      - 8080:8080
    networks: 
      - backend
  
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - ./db/redis:/data
    ports:
      - 6379:6379

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 8081:15672
    networks:
      - backend

networks:
  backend:
    driver: bridge